spring:
  profiles:
    active: local
    group:
      local:
        - comm-local
        - kafka-local

---

spring:
  profiles:
    active: dev
    group:
      dev:
        - comm-dev
        - kafka-dev

